{
  "name": "elm-tailwind-origami",
  "version": "0.0.1",
  "author": "Philipp Krüger",
  "description": "Generate Elm code for Tailwind Utilities and Components that purges using Elm's --optimize!",
  "repository": {
    "type": "git",
    "url": "https://github.com/matheus23/elm-tailwind-origami"
  },
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "yarn test:clean && yarn test:elm-compile && yarn jest",
    "jest": "node --experimental-vm-modules node_modules/jest/bin/jest.js --config test/jest.config.json",
    "test:clean": "rm -rf test/src && rm -rf test/gen && rm -rf test/dist",
    "test:elm-compile": "node --trace-warnings --unhandled-rejections=strict test/test-run.js && cd test && elm make Main.elm --output=/dev/null",
    "test:node-only": "node --trace-warnings --unhandled-rejections=strict test/test-run.js"
  },
  "keywords": [
    "postcss",
    "postcss-plugin",
    "elm",
    "tailwind",
    "tailwindcss"
  ],
  "license": "MIT",
  "peerDependencies": {
    "tailwindcss": "^1.6.2"
  },
  "devDependencies": {
    "@tailwindcss/ui": "^0.6.0",
    "autoprefixer": "^9.8.6",
    "css-what": "^3.3.0",
    "deep-equal": "^2.0.3",
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "fs-extra": "^9.0.1",
    "jest": "^26.4.0",
    "lodash": "^4.17.20",
    "postcss-cli": "^7.1.1",
    "tailwindcss": "^1.7.5"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  }
}
